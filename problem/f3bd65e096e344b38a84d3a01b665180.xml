<problem display_name="Q6-2" markdown="null" weight="6.0">
  <text>
    <p>
    </p>
    <p>Write a method in <code>Bag</code> such that if <code>b1</code> and <code>b2</code> were bags then <code>b1+b2</code> gives a new bag representing the union of the two bags. </p>
    <ul>
      <li>For example,
    <pre>a = Bag()
a.insert(4)
a.insert(3)
b = Bag()
b.insert(4)
print(a+b)
</pre>
prints
<pre>
3:1
4:2
</pre>  
      </li>
    </ul>
    <p>Paste your entire class for <code>Bag</code> with the new method, including the definition, in the box below. Do not leave any debugging print statements.</p>
  </text>
  <coderesponse queuename="Watcher-MITx-6.0001r">
    <textbox rows="30" cols="80" mode="python" tabsize="4"/>
    <codeparam>
      <grader_payload>
{"grader": "quiz/60001_s2017/bag_sum/grade_bag_sum.py", "hide_output": false}
</grader_payload>
    </codeparam>
  </coderesponse>
  <p><b>Note: An L379 or L397 error:</b> means that your code has an infinite loop for some test case. You will receive a zero for this problem if your code has an infinite loop.
  </p>
  <p><b>Note on Score Shown:</b> Your code will ultimately be run on our test cases. Getting "Correct"/green check during the exam does not mean you will get full points. The test cases here ensures you do not have a copy/paste error from your IDE.
  </p>
</problem>
