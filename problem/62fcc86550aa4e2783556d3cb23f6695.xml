<problem display_name="Problem 1" markdown="null" max_attempts="0" showanswer="never" weight="3.0">
  <text>
    <p>
      <i>
   Given a variable x of type int (assume greater or equal to 0), write a program that prints the integers from x to 0, inclusive, each on a separate line. </i>
    </p>
    <p>For example</p>
    <pre># when x = 0, prints
0
# when x == 3, prints
3
2
1
0
    </pre>
    <p>
Enter your Python code for problem 1 here.  Click the blue Submit button to make sure that you copied your code correctly and that it passes our basic tests.
    </p>
    <p>Do not include <code>input</code> statements or define <code>x</code>. Our graders will provide values for you, so write your code assuming that <code>x</code> is already defined.
    </p>
  </text>
  <coderesponse queuename="Watcher-MITx-6.0001r">
    <textbox rows="10" cols="80" mode="python" tabsize="4"/>
    <codeparam>
      <initial_display/>
      <grader_payload>
        {"grader": "microquiz/s2018/quiz1/loopback/grade_loopback_student.py"}
	</grader_payload>
    </codeparam>
  </coderesponse>
  <p><b>Note: An L379 or L397 error:</b> means that your code has an infinite loop for some test case. You will receive a zero for this problem if your code has an infinite loop.
  </p>
  <p><b>Note on Score Shown:</b> Your code will ultimately be run on our test cases. Getting "Correct"/green check during the exam does not mean you will get full points. The test cases here ensures you do not have a copy/paste error from your IDE.
  </p>
</problem>
